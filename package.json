{
  "name": "attrecto-academy-json-server-badget",
  "version": "1.0.0-SNAPSHOT",
  "description": "Attrecto Academy badge mock api",
  "main": "index.js",
  "scripts": {
    "build:production": "echo \"Skipping build stage, because we don't need it... \"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "start-routes": "json-server --watch db.json --port 3001 --routes routes.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "crypto-js": "^4.0.0"
  },
  "devDependencies": {
    "json-server": "^0.16.1"
  }
}
